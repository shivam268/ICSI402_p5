/*
Parikh Shivam
ICSI 402
Assignment 5
UNIX ID: sp191221
*/

void processCompression1(char *fileName); // processes compression takes archive name and filename as parameter

void processCompression2(char *fileName); // process compression 2nd half

unsigned int fTell(char *fileSize); // returns the size of file

void processExtraction(char *archFile); // processes the extraction

void addFileInfo(unsigned char nameSize, char *fileName, unsigned int  fileSize); // adds file info onto array of structs
         

void extractFileCreation(); // creates file for extraction
